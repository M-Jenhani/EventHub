services:
  - type: web
    name: eventhub-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build --prod
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
